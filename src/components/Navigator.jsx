import React from 'react';

const Navigator = ({props}) => {

    return (
        <div>
            <nav style={{height: '100px'}} className="round-corners navbar  navbar-dark bg-gray justify-content-center">
                <h3 className="">COVID-19 : Live Situational Analysis Dashboard</h3>
            </nav>
        </div>
    );
}

export default Navigator;